server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback -- client-side routing (Angular/React router) needs this.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
